pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        jcenter()
    }
}

rootProject.name = ("ir.amv.enterprise.locorepo.client.gradle")

include(":plugin")
